export class Task {

    constructor(title : string,
        description: string,
        assignedTo: string,
        createdAt: string,
        priority: string, 
        status: string,
        id?:string){
    }
}